# Build output
build/

# Device identity: machine key, node key and disco key in the clear.
# Anything holding these can act as this node on the tailnet.
tsesp-keys.bin
*.bin

# ESP-IDF
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
